% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_gui.R, R/utils.R
\name{create_action}
\alias{create_action}
\title{Create and manage undo/redo action}
\usage{
create_action(type, data, reverse_data = NULL)

create_action(type, data, reverse_data = NULL)
}
\arguments{
\item{type}{Character string specifying the type of action}

\item{data}{List containing the action data}

\item{reverse_data}{Optional list containing data for reversing the action}
}
\value{
List containing:
\itemize{
\item type: Action type identifier
\item data: Action data
\item reverse_data: Data for reversing the action
\item timestamp: Time the action was created
}

List containing:
\itemize{
\item type: Action type identifier
\item data: Action data
\item reverse_data: Data for reversing the action
\item timestamp: Time the action was created
}
}
\description{
Creates an action object for the undo/redo system, containing information about
the type of action, the data involved, and how to reverse the action.

Creates an action object for the undo/redo system, containing information about
the type of action, the data involved, and how to reverse the action.
}
\examples{
\dontrun{
# Create an add annotation action
action <- create_action(
  type = "add_annotation",
  data = list(
    start = 1,
    end = 10,
    code = "important"
  )
)

# Create a merge codes action
merge_action <- create_action(
  type = "merge_codes",
  data = list(
    old_codes = c("code1", "code2"),
    new_code = "merged_code"
  )
)
}

\dontrun{
# Create an add annotation action
action <- create_action(
  type = "add_annotation",
  data = list(
    start = 1,
    end = 10,
    code = "important"
  )
)

# Create a merge codes action
merge_action <- create_action(
  type = "merge_codes",
  data = list(
    old_codes = c("code1", "code2"),
    new_code = "merged_code"
  )
)
}

}
\keyword{internal}
