% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_gui.R, R/visualization.R
\name{plot_sequence_patterns}
\alias{plot_sequence_patterns}
\title{Plot code sequence patterns}
\usage{
plot_sequence_patterns(sequences, main = "", ...)

plot_sequence_patterns(sequences, main = "", ...)
}
\arguments{
\item{sequences}{List containing sequence information:
\itemize{
\item transitions: List of code transitions
}}

\item{main}{Character string for plot title}

\item{...}{Additional arguments passed to barplot()}
}
\value{
Invisible NULL, called for side effect of creating plot

Invisible NULL, called for side effect of creating plot
}
\description{
Creates a barplot visualization of code sequence patterns, showing the frequency
of different code transitions with rotated labels for better readability.

Creates a barplot visualization of code sequence patterns, showing the frequency
of different code transitions with rotated labels for better readability.
}
\examples{
\dontrun{
sequences <- list(
  transitions = list(
    c(from = "A", to = "B"),
    c(from = "B", to = "C")
  )
)
plot_sequence_patterns(sequences, main = "Code Sequences")
}
\dontrun{
sequences <- list(
  transitions = list(
    c(from = "A", to = "B"),
    c(from = "B", to = "C")
  )
)
plot_sequence_patterns(sequences, main = "Code Sequences")
}
}
\keyword{internal}
