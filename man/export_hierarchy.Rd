% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_gui.R, R/hierarchy.R
\name{export_hierarchy}
\alias{export_hierarchy}
\title{Export code hierarchy to JSON format}
\usage{
export_hierarchy(node)

export_hierarchy(node)
}
\arguments{
\item{node}{Root node of the hierarchy tree}
}
\value{
JSON string representation of the hierarchy

JSON string representation of the hierarchy
}
\description{
Converts the code hierarchy tree structure into a JSON string representation
that can be saved or transmitted while preserving all node properties and
relationships.

Converts the code hierarchy tree structure into a JSON string representation
that can be saved or transmitted while preserving all node properties and
relationships.
}
\examples{
\dontrun{
# Create sample hierarchy
root <- Node$new("Root")
theme1 <- root$AddChild("Theme1")
theme1$AddChild("Code1")

# Export to JSON
json_string <- export_hierarchy(root)
writeLines(json_string, "hierarchy.json")
}

\dontrun{
# Create sample hierarchy
root <- Node$new("Root")
theme1 <- root$AddChild("Theme1")
theme1$AddChild("Code1")

# Export to JSON
json_string <- export_hierarchy(root)
writeLines(json_string, "hierarchy.json")
}

}
\keyword{internal}
