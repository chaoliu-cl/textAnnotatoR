% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_gui.R, R/hierarchy.R
\name{get_code_names}
\alias{get_code_names}
\title{Get all code names from hierarchy}
\usage{
get_code_names(node)

get_code_names(node)
}
\arguments{
\item{node}{Root node of the code hierarchy (data.tree Node object)}
}
\value{
Character vector containing all code names in the hierarchy

Character vector containing all code names in the hierarchy
}
\description{
Recursively extracts all code names from a code hierarchy tree structure,
traversing through all nodes and collecting their names.

Recursively extracts all code names from a code hierarchy tree structure,
traversing through all nodes and collecting their names.
}
\examples{
\dontrun{
# Create a simple code hierarchy
root <- Node$new("Root")
theme1 <- root$AddChild("Theme1")
theme1$AddChild("Code1")
theme1$AddChild("Code2")

# Get all code names
codes <- get_code_names(root)
print(codes)  # c("Root", "Theme1", "Code1", "Code2")
}

\dontrun{
# Create a simple code hierarchy
root <- Node$new("Root")
theme1 <- root$AddChild("Theme1")
theme1$AddChild("Code1")
theme1$AddChild("Code2")

# Get all code names
codes <- get_code_names(root)
print(codes)  # c("Root", "Theme1", "Code1", "Code2")
}

}
\keyword{internal}
