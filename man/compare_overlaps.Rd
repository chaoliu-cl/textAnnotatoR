% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_gui.R, R/comparison.R
\name{compare_overlaps}
\alias{compare_overlaps}
\title{Compare overlap patterns between coders}
\usage{
compare_overlaps(coding_strategies)

compare_overlaps(coding_strategies)
}
\arguments{
\item{coding_strategies}{List of coding strategies, where each strategy contains
overlap information}
}
\value{
List containing:
\itemize{
\item total_overlaps_range: Range of total overlaps across strategies
\item unique_pairs_range: Range of unique code pairs across strategies
}

List containing:
\itemize{
\item total_overlaps_range: Range of total overlaps across strategies
\item unique_pairs_range: Range of unique code pairs across strategies
}
}
\description{
Analyzes how different coders overlap in their code applications by comparing
overlap patterns and frequencies across coding strategies.

Analyzes how different coders overlap in their code applications by comparing
overlap patterns and frequencies across coding strategies.
}
\examples{
\dontrun{
strategy1 <- list(
  co_occurrences = list(
    combinations = list(
      frequencies = c("code1-code2" = 2)
    )
  )
)
strategy2 <- list(
  co_occurrences = list(
    combinations = list(
      frequencies = c("code2-code3" = 1)
    )
  )
)
results <- compare_overlaps(list(strategy1, strategy2))
}

\dontrun{
strategy1 <- list(
  co_occurrences = list(
    combinations = list(
      frequencies = c("code1-code2" = 2)
    )
  )
)
strategy2 <- list(
  co_occurrences = list(
    combinations = list(
      frequencies = c("code2-code3" = 1)
    )
  )
)
results <- compare_overlaps(list(strategy1, strategy2))
}

}
\keyword{internal}
