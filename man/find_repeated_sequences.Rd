% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R, R/annotate_gui.R
\name{find_repeated_sequences}
\alias{find_repeated_sequences}
\title{Find repeated patterns in code sequences}
\usage{
find_repeated_sequences(annotations)

find_repeated_sequences(annotations)
}
\arguments{
\item{annotations}{Data frame of sorted annotations with columns:
\itemize{
\item code: character, code identifier
}}
}
\value{
Named list of pattern frequencies where:
\itemize{
\item names: Code patterns (e.g. "code1-code2")
\item values: Number of occurrences
}

Named list of pattern frequencies where:
\itemize{
\item names: Code patterns (e.g. "code1-code2")
\item values: Number of occurrences
}
}
\description{
Identifies repeating patterns of 2-3 codes in sequence to uncover recurring
coding structures.

Identifies repeating patterns of 2-3 codes in sequence to uncover recurring
coding structures.
}
\examples{
\dontrun{
annotations <- data.frame(
  code = c("A", "B", "A", "B", "C")
)
patterns <- find_repeated_sequences(annotations)
# Returns list showing A-B pattern occurs twice
}
\dontrun{
annotations <- data.frame(
  code = c("A", "B", "A", "B", "C")
)
patterns <- find_repeated_sequences(annotations)
# Returns list showing A-B pattern occurs twice
}
}
\keyword{internal}
